const expect      = require("chai").expect;
const DataService = require('../../app/services/data-service');

describe('Data Service', () => {

  it('should get data for the specified city', (done) => {
    done();
  });

});
